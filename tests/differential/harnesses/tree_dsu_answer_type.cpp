#include <bits/stdc++.h>
using namespace std;

// @@@TEMPLATE@@@

static_assert(
    is_same_v<typename decltype(ans)::value_type, long long>,
    "DSU answer value_type must preserve n^2");

int main() { return 0; }
