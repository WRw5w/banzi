# 《合订本.pdf》修改意见

## 1. 修改目标

这份意见针对原来的《合订本.pdf》，目标不是把内容扩展成算法百科，而是把它改造成一套真正能在区域赛现场使用的手册。

使用者的实际情况是：

- 当前大约 CF 1700–1800；
- 目标是在赛场遇到银牌档题时，手册里有可以直接复制、理解和改造的模板；
- 很多算法没有独立使用经验，需要依靠手册中的解释、完整题例和输入输出确认代码没有抄错。

因此，手册的判断标准应当是：

> 看到题面关键词后，能否快速定位模板；复制模板后，能否看懂变量含义、完成接入，并用样例和边界数据确认模板可用。

不以“收录算法越多越好”为标准。

---

## 2. 原版本的主要问题

### 2.1 章节是长期拼接形成的，目录层级不稳定

原手册存在以下问题：

- 章节编号有拼接残留；
- 同一类算法被拆到多个不连续的位置；
- 基础内容、进阶内容、补充内容和错题本之间边界不清；
- 目录不能直接帮助现场定位；
- 文件章节名和 PDF 中的章节名不统一。

修改时应当重新生成目录，不能继续在旧目录上局部修补。

### 2.2 同一数据结构存在重复版本

原手册中有同一数据结构重复出现、代码略有差异的情况。这样会造成：

- 不知道哪一份才是主模板；
- 修复一个版本后，另一个版本仍然可能有 bug；
- 现场复制时容易混用变量名和初始化方式；
- 学习时无法判断两个版本的差别是否重要。

修改原则：

- 一个算法只保留一份主模板；
- 确实有必要时，只增加一个明确标注用途的变体；
- 其他位置只写“见主模板”和差异说明，不再复制整段代码。

### 2.3 高难算法只有代码，没有使用说明

原版本中 AC 自动机等算法存在“代码看起来完整，但不知道怎么接题”的问题。

对于 AC 自动机，至少要解释：

- Trie 节点表示什么；
- `fail` 指针表示什么；
- `terminal`、`output` 分别统计什么；
- 为什么要建立 fail 指针；
- `build()` 什么时候调用；
- 多测时如何清空；
- 总出现次数和每个模式串出现次数为什么是两种不同写法；
- 模式串互为后缀时如何处理。

SAM、后缀数组、PAM、可持久化结构、DSU on Tree、CHT 等高风险模板也必须遵循同样标准。

### 2.4 代码依赖隐藏在其他章节

原版本的一些代码默认依赖前面出现过的：

- `INF`；
- `MAXN`；
- 图结构；
- 线段树对象；
- 并查集；
- 全局数组；
- 初始化逻辑。

现场复制时，如果只复制当前算法代码，容易出现无法编译或变量语义不一致的问题。

修改后，每个算法卡片必须明确：

- 依赖哪一份基础模板；
- 新增哪些头文件或类型；
- 新增哪些全局数组和容器；
- 节点池或数组需要开多大；
- 初始化函数是什么；
- 主调用方式是什么；
- 输入输出如何接入。

### 2.5 代码注释不足

代码不能只依靠标题和一两句说明。至少要在代码中说明：

- 数组、结构体字段、节点编号的含义；
- 每个循环或递归维护的不变量；
- 为什么要进行某个转移；
- 哪些地方必须先初始化或清空；
- 哪些地方最容易写错；
- 哪些条件不满足时不能使用模板。

注释应服务于理解代码，不写无关的历史介绍。

### 2.6 难度较高的模板缺少完整题例

原版本中有些算法只有类似“调用 `foo()` 看输出”的测试，不能满足现场使用要求。

对于 CF 1800 左右、区域赛铜牌以上或银牌档常见算法，必须给出：

- 题目要解决什么问题；
- 数据范围；
- 输入格式；
- 输出格式；
- 完整样例输入；
- 完整样例输出；
- 题面变量如何映射到模板；
- 至少四组边界测试；
- 多测时如何重新初始化。

测试示例可以放在同一张模板卡片中，不需要把单测和多测拆成两个章节。

---

## 4. 统一代码地基

所有模板直接以 `muban.cpp` 为地基，不额外维护另一份基础头文件。

地基统一采用：

```cpp
#include <bits/stdc++.h>
using namespace std;

#define int long long
#define endl '\n'

using i64 = long long;
using i128 = __int128_t;

// 别把这个改成具体的数,比赛的时候谁有时间给你重写
const int inf = 2e18 + 10;
const int N = 1e6 + 10;
const double eps = 1e-8;
```

统一规则：

- 普通整数默认写 `int`，由宏映射为 `long long`；
- 主函数写 `signed main()`；
- 位移常数使用 `1LL`；
- 本次重构默认不处理 `unsigned` 版本；
- 需要更大中间结果时使用 `i128`；
- 每个算法说明是否需要覆盖 `N`、`inf`、`eps`；
- 代码不能依赖其他章节中偶然出现的同名变量。

“代码独立可用”的含义是：

> 将 `muban.cpp` 与该算法卡片中的追加代码放到同一个源文件后，可以按照卡片中的接口编译和运行。

---

```text
g++ -std=c++17 -O2 -Wall
```

并检查：

- 基础地基能编译；
- 每个高风险模板的代码块没有隐藏依赖；
- 样例输入输出与代码接口一致；
- 空、单点、重复、无解、不连通、重边、自环等边界被覆盖；
- 两本 PDF 的目录正常生成；
- 章节名、文件名和目录条目一致。

最终标准不是“比原版多了多少算法”，而是：

> 在区域赛现场遇到银牌档题时，能快速判断手册中是否有对应模板；如果有，能够看懂、复制、接入，并用样例和边界数据确认没有抄错。
