#include <bits/stdc++.h>
using namespace std;

// 统一地基：所有手册卡片都从本文件复制后追加。
#define int long long
#define endl '\n'

using i64 = long long;
using i128 = __int128_t;

// 别把这个改成具体的数,比赛的时候谁有时间给你重写
const int inf = 2e18 + 10;
const int N = 1e6 + 10;
const double eps = 1e-8;

void solve() {
    // 默认按多测模板书写；单测题目把 t 改成 1，并删除对应读入即可。
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    cin >> t;
    while (t--) solve();
    return 0;
}
